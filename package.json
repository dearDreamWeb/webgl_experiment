{
  "name": "react-webgl",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist && tsc && vite build && yarn pages",
    "serve": "vite preview",
    "pages": "cd dist && git init && git add . && git commit -m 'feat: build' && git remote add origin https://github.com/dearDreamWeb/webgl_experiment.git && git push -f origin master:dist"
  },
  "dependencies": {
    "@types/earcut": "^2.1.1",
    "@types/node": "^16.11.4",
    "earcut": "^2.2.3",
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "tess2": "^1.0.0"
  },
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "@vitejs/plugin-react": "^1.0.0",
    "typescript": "^4.3.2",
    "vite": "^2.6.4"
  }
}